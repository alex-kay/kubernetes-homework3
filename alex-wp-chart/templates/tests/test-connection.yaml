apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "alex-wp-chart.fullname" . }}-test-connection"
  labels:
    {{- include "alex-wp-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "alex-wp-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
